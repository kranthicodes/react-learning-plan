import React, { Fragment } from "react";
import spinner from "./spinner.gif";

const Spinner = () => (
  <Fragment>
    <h2 className="display-4 text-center">Loading...</h2>
  </Fragment>
);

export default Spinner;
